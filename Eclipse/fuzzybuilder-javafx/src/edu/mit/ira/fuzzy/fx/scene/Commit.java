package edu.mit.ira.fuzzy.fx.scene;
import edu.mit.ira.fuzzy.fx.base.Container2D;

public class Commit extends Container2D {
	
	/**
 	 * Instantiate a new Commit Container with a given ID and friendly name
 	 * 
 	 * @param id
 	 * @param friendlyName
 	 */
	public Commit(String id, String friendlyName) {
		super(id, friendlyName);
	}
}