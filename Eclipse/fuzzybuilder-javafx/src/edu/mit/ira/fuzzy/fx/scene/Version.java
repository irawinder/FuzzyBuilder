package edu.mit.ira.fuzzy.fx.scene;
import edu.mit.ira.fuzzy.fx.base.Container2D;

public class Version extends Container2D {
	
	/**
 	 * Instantiate a new Version Container with a given ID and friendly name
 	 * 
 	 * @param id
 	 * @param friendlyName
 	 */
	public Version(String id, String friendlyName) {
		super(id, friendlyName);
	}
}