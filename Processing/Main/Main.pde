

// Runs Once On Program Start
//
void setup() {
  size(400, 400);
  init();
  render();
}

// Runs on Infinite Loop after setup() completes
//
void draw() {
  
}

void keyPressed() {
  init();
  render();
}
